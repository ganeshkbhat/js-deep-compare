
// (a = new Error() ) instanceof Error
// (a = new AggregateError ) instanceof AggregateError
// (a = new EvalError ) instanceof EvalError
// (a = new RangeError ) instanceof RangeError
// (a = new ReferenceError ) instanceof ReferenceError
// (a = new SyntaxError ) instanceof SyntaxError
// (a = new TypeError ) instanceof TypeError
// (a = new URIError ) instanceof URIError
// (a = new InternalError ) instanceof InternalError
// (a = new BigInt ) instanceof BigInt
// (a = new Math ) instanceof Math
// (a = new Date ) instanceof Date
// (a = new RegExp ) instanceof RegExp

// (a = new Int8Array ) instanceof Int8Array
// (a = new Uint8Array ) instanceof Uint8Array
// (a = new Uint8ClampedArray ) instanceof Uint8ClampedArray
// (a = new Int16Array ) instanceof Int16Array
// (a = new Uint16Array ) instanceof Uint16Array
// (a = new Int32Array ) instanceof Int32Array
// (a = new Uint32Array ) instanceof Uint32Array
// (a = new BigInt64Array ) instanceof BigInt64Array
// (a = new BigUint64Array ) instanceoBigUint64Arrayf 
// (a = new Float32Array ) instanceof Float32Array
// (a = new Float64Array ) instanceof Float64Array

// (a = new WeakSet ) instanceof WeakSet
// (a = new Set ) instanceof Set

// (a = new ArrayBuffer ) instanceof ArrayBuffer
// (a = new SharedArrayBuffer ) instanceof SharedArrayBuffer
// (a = new DataView ) instanceof DataView
// (a = new Atomics ) instanceof Atomics
// (a = new JSON ) instanceof JSON

// (a = new WeakRef ) instanceof WeakRef
// (a = new FinalizationRegistry ) instanceof FinalizationRegistry

// (a = new Iterator ) instanceof Iterator
// (a = new AsyncIterator ) instanceof AsyncIterator
// (a = new Promise ) instanceof Promise
// (a = new GeneratorFunction ) instanceof GeneratorFunction
// (a = new AsyncGeneratorFunction ) instanceof AsyncGeneratorFunction
// (a = new Generator ) instanceof Generator
// (a = new AsyncGenerator ) instanceof AsyncGenerator
// (a = new AsyncFunction ) instanceof AsyncFunction

// (a = new Reflect ) instanceof Reflect
// (a = new Proxy ) instanceof Proxy

// (a = new Intl ) instanceof Intl
// (a = new Intl.Collator ) instanceof Intl.Collator
// (a = new Intl.DateTimeFormat ) instanceof Intl.DateTimeFormat
// (a = new Intl.DisplayNames ) instanceof Intl.DisplayNames
// (a = new Intl.DurationFormat ) instanceof Intl.DurationFormat
// (a = new Intl.ListFormat ) instanceof Intl.ListFormat
// (a = new Intl.Locale ) instanceof Intl.Locale
// (a = new Intl.NumberFormat ) instanceof Intl.NumberFormat
// (a = new Intl.PluralRules ) instanceof Intl.PluralRules
// (a = new Intl.RelativeTimeFormat ) instanceof Intl.RelativeTimeFormat
// (a = new Intl.Segmenter ) instanceof Intl.Segmenter

// (a = new WebAssembly ) instanceof WebAssembly

// (a = new Worker ) instanceof Worker
// (a = new ServiceWorkerContainer ) instanceof ServiceWorkerContainer
// (a = new ServiceWorkerGlobalScope ) instanceof ServiceWorkerGlobalScope

// (a = new FetchEvent ) instanceof FetchEvent
// (a = new Event ) instanceof Event

// (a = new globalThis ) instanceof globalThis
// (a = new global ) instanceof global

// (a = new DOM ) instanceof DOM
// (a = new Navigator ) instanceof Navigator
// 
// (a = new Window ) instanceof Window
